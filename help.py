help_msg = (
    'Usage: python3 main.py [options]\n'
    'Options:\n'
    '\t-h, --help\t\t\tShow this help message\n'
    '\t-H --headless\t\t\tRun in headless mode (browser GUIs will not open)\n'
    '\t-s, --silent\t\t\tSkip all prints except when bot is initialized and when queue restarts or bot exits\n'
    '\t-S, --super-silent\t\tThe console won\'t output anything at all. Not sure why you\'d want this, but just in case\n'
    '\t-r, --run-once\t\t\tWhen the "Enter" button is re-enabled after being clicked, exit rather than looping\n'
)